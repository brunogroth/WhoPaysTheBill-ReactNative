import { StyleSheet, Text, View } from 'react-native';
const UserStage = () => {
    return (
        <View>
            <Text>This is the user stage.</Text>
        </View>
    );
}

export default UserStage;